export default class Neo{
  constructor(sentryId, fullname, minYears, maxYears) {
    this.sentryId = sentryId
    this.fullName = fullname
    this.minYears = minYears
    this.maxYears = maxYears
  }
}